Upload your `.jpg`, `.png`, or `.webp` image files directly into this `/public/photography/` folder. 

The Photography page is coded to automatically detect any images placed here and immediately render them into the gallery grid, without you needing to touch any code!
